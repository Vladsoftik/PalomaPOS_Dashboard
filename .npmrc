# Configuration for npm install
# Note: Deprecation warnings from transitive dependencies (rimraf, glob, etc.) are harmless
# and don't affect deployment. These come from npm's own dependencies.
audit-level=moderate

